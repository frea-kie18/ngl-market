# Termux / Hardhat Commands

# Compile
npx hardhat compile

# Deploy to Mumbai
npx hardhat run scripts/deploy.js --network mumbai

# Clean build artifacts
npx hardhat clean

# Check contract address in deploy output